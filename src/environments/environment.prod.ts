/** URL base del API (opcional). En producción suele ser el mismo host con puerto 3012 o una URL fija. */
export const environment = {
  production: true,
  backendPort: 3012,
  apiBaseUrl: undefined as string | undefined
};
